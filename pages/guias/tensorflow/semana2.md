El dataset MINIST esta disponible en

```
tf.keras.datasets.fashion_mnist
```

Si tenes una imagen en escala de grises, podes dibujarla con matplotlib para ver su forma visual.

```
plt.imshow(training_images[0])
```
